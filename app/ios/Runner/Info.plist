<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CADisableMinimumFrameDurationOnPhone</key>
	<true/>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>Personal Crm</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>personal_crm</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(FLUTTER_BUILD_NAME)</string>
	<key>CFBundleSignature</key>
	<string>????</string>
	<key>CFBundleVersion</key>
	<string>$(FLUTTER_BUILD_NUMBER)</string>
	<!-- Export compliance: answers App Store Connect's encryption question once,
	     instead of on every upload. NO is correct because the only encryption in
	     this app is HTTPS to the sync server and the OS Keychain for the sign-in
	     token — standard encryption that is exempt.
	     ⚠ REVISIT IF END-TO-END ENCRYPTION IS EVER ADDED (the parked design in
	     the handoff). Encrypting synced data with the app's own crypto is not
	     "HTTPS only" any more, and this answer would need to be re-checked. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<!-- ⚠ Required by the "Add to calendar" row on a meeting. iOS opens its own
	     pre-filled new-event screen (EKEventEditViewController) and the user
	     confirms; this app never reads the calendar and never writes without
	     that tap. The string is what the permission sheet shows, so it says
	     what actually happens. -->
	<key>NSCalendarsUsageDescription</key>
	<string>Personal adds a meeting to your calendar only when you tap Add to calendar, and never reads what is already there.</string>
	<key>NSCalendarsWriteOnlyAccessUsageDescription</key>
	<string>Personal adds a meeting to your calendar only when you tap Add to calendar, and never reads what is already there.</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
		<key>UISceneConfigurations</key>
		<dict>
			<key>UIWindowSceneSessionRoleApplication</key>
			<array>
				<dict>
					<key>UISceneClassName</key>
					<string>UIWindowScene</string>
					<key>UISceneConfigurationName</key>
					<string>flutter</string>
					<key>UISceneDelegateClassName</key>
					<string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
					<key>UISceneStoryboardFile</key>
					<string>Main</string>
				</dict>
			</array>
		</dict>
	</dict>
	<key>UIApplicationSupportsIndirectInputEvents</key>
	<true/>
	<!-- ⚠ Without these, canLaunchUrl() reports false for WhatsApp and WeChat
	     even when both are installed. wa.me is the core interaction of the core
	     loop; weixin:// is the WeChat fallback that opens the app but cannot
	     target a chat. -->
	<key>LSApplicationQueriesSchemes</key>
	<array>
		<string>whatsapp</string>
		<string>weixin</string>
	</array>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
</dict>
</plist>
